import React from "react";
import PropTypes from "prop-types";

FormTourist.propTypes = {
  tourist: PropTypes.array,
};

FormTourist.defaultProps = {
  tourist: null,
};

function FormTourist(props) {
  const { tourist } = this.props;
  return (<div>
      
    </div>);
}

export default FormTourist;
